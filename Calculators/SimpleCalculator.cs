// How to find this class (when importing with 'using')
namespace Calculating.Calculators; // Directly linked to what folder its in, purely here to avoid name conflicts when referenced.

public class SimpleCalculator
{
    public static int Add(int lhs, int rhs)
    {
        return lhs + rhs;
    }
}
